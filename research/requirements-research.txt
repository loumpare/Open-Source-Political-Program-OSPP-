# OSPP Research Pipeline
# conda activate ospp && pip install -r research/requirements-research.txt

# PDF download
requests==2.32.4
tqdm==4.67.1

# PDF text extraction
pdfplumber==0.11.4
pypdf==5.1.0

# Google Drive sync
rclone-python==0.1.10  # thin wrapper; rclone binary must be installed separately

# Embeddings & vector DB
sentence-transformers==3.3.1
chromadb==0.5.23

# Anthropic (for LLM-assisted chunking if needed)
anthropic==0.40.0

# Utilities
python-dotenv==1.0.1
